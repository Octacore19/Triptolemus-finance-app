<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    xmlns:tools="http://schemas.android.com/tools"
    package="com.chegsmania.triptotracker">

    <application
        android:allowBackup="false"
        android:icon="@mipmap/ic_launcher_icon"
        android:label="@string/app_name"
        android:roundIcon="@mipmap/ic_launcher_icon_round"
        android:supportsRtl="true"
        android:theme="@style/AppTheme.NoActionBar"
        tools:ignore="GoogleAppIndexingWarning">
        <activity
            android:name=".views.activities.DashboardActivity"
            android:theme="@style/AppTheme.NoActionBar.Dashboard" />
        <activity android:name=".views.activities.PasswordActivity" />
        <activity android:name=".views.activities.SignupActivity" />
        <activity android:name=".views.activities.SigninActivity" />
        <activity android:name=".views.activities.LandingActivity">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />

                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>

        <meta-data
            android:name="preloaded_fonts"
            android:resource="@array/preloaded_fonts" />
    </application>

</manifest>